node_modules/
/build
/.svelte-kit
/data
# compose.dev.yaml points npm's cache here so it survives between container
# runs. It is build scratch, not source.
/.npm-cache
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
.DS_Store
